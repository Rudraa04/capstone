//NOTICE:- DONT DELETE THIS FILE AS ITS FOR TESTING PURPOSES. HUKAM THI - HARSHIL RATHOD
{/*import dotenv from "dotenv";
dotenv.config();

import { sendMail } from "../utils/mailer.js";

const to = process.env.MAIL_FROM_ADDRESS; // send to yourself
const subject = "✅ Test from Patel Ceramics backend";
const html = `<p>Hello! This is a test email sent from your backend via Gmail SMTP.</p>
<p>If you can read this, SMTP is working. 🎉</p>`;

try {
  const info = await sendMail({ to, subject, html });
  console.log("Sent! MessageId:", info.messageId);
} catch (err) {
  console.error("Send failed:", err);
  process.exitCode = 1;
}
*/}

//this code is to test mail generating from backend and going to help.patelceramics@gmail.com
// if anyhow someday gmail stops working then we can test it with running the command node scripts/mailTest.mjs in backend folder
